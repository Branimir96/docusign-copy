[phases.setup]
nixPkgs = ['nodejs_20', 'openssl']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = [
  'npx prisma generate --schema=./packages/prisma/schema.prisma',
  'npm run build'
]

[start]
cmd = 'npx prisma migrate deploy --schema=./packages/prisma/schema.prisma && npm start'